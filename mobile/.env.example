# URL base da API VClass (backend Hono/Cloudflare). Em dispositivo físico ou
# emulador, "localhost" não aponta para a tua máquina de desenvolvimento —
# usa o IP da tua máquina na rede local.
EXPO_PUBLIC_API_URL=http://localhost:3000
