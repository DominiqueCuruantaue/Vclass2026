{
  "name": "vclass-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Wrapper Android (Capacitor) da app web VClass — carrega https://vclass.co.mz numa WebView nativa para garantir paridade total de layout com a versão web.",
  "scripts": {
    "sync": "cap sync android",
    "open:android": "cap open android",
    "build:android": "cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.2",
    "@capacitor/splash-screen": "^8.0.2",
    "@capacitor/status-bar": "^8.0.3"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.5.2",
    "typescript": "^5.9.3"
  }
}
