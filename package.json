{
  "name": "vclass",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:sandbox": "wrangler pages dev dist --ip 0.0.0.0 --port 3000",
    "build": "vite build",
    "postbuild": "node -e \"const fs=require('fs'),p=require('path');fs.readdirSync('dist').filter(f=>f.endsWith('.html')&&f!=='designs').forEach(f=>{const fp=p.join('dist',f);if(fs.statSync(fp).isFile())fs.unlinkSync(fp);});console.log('postbuild: HTMLs estáticos removidos de dist/')\"",
    "preview": "wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist",
    "deploy:prod": "npm run build && wrangler pages deploy dist --project-name vclass",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "clean-port": "fuser -k 3000/tcp 2>/dev/null || true",
    "test": "curl http://localhost:3000",
    "git:commit": "git add . && git commit -m"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "bcryptjs": "^2.4.3",
    "hono": "^4.12.12",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "@types/bcryptjs": "^2.4.6",
    "@types/jsonwebtoken": "^9.0.5",
    "typescript": "^5.3.3",
    "vite": "^6.3.5",
    "vite-plugin-static-copy": "^3.4.0",
    "wrangler": "^4.4.0"
  }
}
